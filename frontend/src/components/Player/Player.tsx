import React from 'react'
import { motion } from 'framer-motion'
import '../../style/Player.scss'

function Player() {
  return (
    <motion.div className="player">
        <span>Player Component</span>
    </motion.div>
  )
}

export default Player
